package patterns.persist.service.impl;

import patterns.persist.entity.LegalEntity;
import patterns.persist.service.DealItem;

public class LegalEntityImp implements DealItem<LegalEntity> {
    @Override
    public void update(LegalEntity item) {

    }
}
